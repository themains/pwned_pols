pandas
everypolitician
numpy
pyjanitor
tqdm
pycountry
fastparquet
pyarrow
email-validator
# 05 reads data/danish_parliament_1_2025.xlsx via openpyxl; without it the
# notebook stops at its import cell.
openpyxl
# figures in 07, 09 and 10
matplotlib
# DNS MX lookups in 06_validate_email_domains
dnspython
# `make analysis` executes the notebooks headless
jupyter
nbconvert
ipykernel
# 11_crosscountry: OLS for the country-level predictors table
statsmodels
# 11_crosscountry also needs dominance-analysis for the Shapley decomposition,
# but it calls numpy.bool8, removed in numpy 2.0, so it will not import on a
# current environment. Install it in a separate venv pinned to numpy<2 if you
# need to regenerate figures/fig_shapley.pdf; the committed figure came from
# such an environment. Left out here rather than pinning numpy<2 repo-wide.
# dominance-analysis
